# Railway Environment Configuration
# Copy these to Railway Dashboard → Settings → Environment Variables

# Server Configuration (Railway auto-sets PORT)
API_HOST=0.0.0.0
WORKERS=2

# Redis Configuration (use Railway Redis addon)
# Railway provides REDIS_URL, extract these values:
REDIS_HOST=${REDIS_HOST}
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=${REDIS_PASSWORD}

# Database Configuration (Railway auto-sets this with PostgreSQL addon)
DATABASE_URL=${DATABASE_URL}

# Authentication (GENERATE NEW SECRET!)
SECRET_KEY=CHANGE_THIS_RUN_openssl_rand_hex_32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Storage Configuration (use local on Railway free tier)
STORAGE_TYPE=local
STORAGE_PATH=/tmp/inference_storage

# Model Configuration
MODEL_CACHE_DIR=/tmp/model_cache
MAX_MODEL_CACHE_SIZE_GB=10

# Worker Configuration (use Railway Redis)
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}
WORKER_CONCURRENCY=1
CELERY_WORKER_POOL=solo

# Inference Configuration (no GPU on Railway free tier)
DEFAULT_TIMEOUT=300
MAX_BATCH_SIZE=4
ENABLE_GPU=false
